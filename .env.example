# Local: SQLite. Vercel: use a PostgreSQL URL (e.g. Neon, Vercel Postgres)
DATABASE_URL="file:./dev.db"
JWT_SECRET="your-super-secret-jwt-key-min-32-chars"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# On Vercel set to "true" to use polling instead of WebSockets (saves bundle + no custom server)
# NEXT_PUBLIC_USE_POLLING="true"

# Google OAuth (personal account - files go to your Drive)
GOOGLE_CLIENT_ID="your-client-id"
GOOGLE_CLIENT_SECRET="your-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"
# Get this by visiting /api/auth/google/setup once
GOOGLE_DRIVE_REFRESH_TOKEN=""

# Optional: use a specific folder as root (otherwise creates CollabDrive in Drive root)
# GOOGLE_DRIVE_ROOT_FOLDER_ID="your-folder-id"
